fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
httpx>=0.27.0
pydantic>=2.0.0

# LLM providers (install as needed)
# openai>=1.30.0          # for GPT-4o / GPT-4V
# ollama                   # for local Ollama models

# Production RAG (optional)
# chromadb>=0.5.0
# sentence-transformers

# Testing
pytest>=8.0.0
